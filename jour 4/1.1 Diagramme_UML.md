```mermaid
classDiagram
    class User {
        +int id
        +string email
        -string password
        +string name
        +DateTime createdAt
        +checkPassword(password) bool
        +getTasks() Task[]
        +getStats() object
    }

    class Task {
        +int id
        +string title
        +string description
        +DateTime dueDate
        +string priority
        +string status
        +DateTime createdAt
        +markAsCompleted() void
        +isOverdue() bool
        +assignCategory(category) void
    }

    class Category {
        +int id
        +string name
        +string color
        +string icon
        +getTasks() Task[]
        +getTaskCount() int
    }

    User "1" --> "0..*" Task : owns
    User "1" --> "0..*" Category : has
    Category "1" --> "0..*" Task : contains